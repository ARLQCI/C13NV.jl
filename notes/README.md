# Generalized Hamiltonian for multiple ¹³C atoms around an NV center.

This folder contains a note written as a [Quarto Markdown `.qmd` Document](https://quarto.org).

Assuming Quarto and LaTeX is installed, it can be rendered to PDF, DOCX, and HTML format.

Run `make all` to generate all output formats.

Run `make preview` to open a preview of the HTML format in the browser that automatically updates as the `.qmd` file is edited.

Run `make help` for further options.
