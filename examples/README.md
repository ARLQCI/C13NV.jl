This folder contains Jupyter notebooks saved via the [Jupytext extension](https://jupytext.readthedocs.io/en/latest/#). To create the matching `.ipynb` files, run

```
make ipynb
```

To batch-execute the notebooks, run

```
make EXECUTE=--execute ipynb
```
